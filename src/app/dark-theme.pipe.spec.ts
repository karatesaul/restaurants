import { DarkThemePipe } from './dark-theme.pipe';

describe('DarkThemePipe', () => {
  it('create an instance', () => {
    const pipe = new DarkThemePipe();
    expect(pipe).toBeTruthy();
  });
});
